# Production Environment - REMOVED

Este ambiente foi removido para simplificar o projeto acadêmico.

Para usar production em um futuro, copie os arquivos de `dev/` e customize com configurações mais robustas.

```bash
cp dev/*.tf production/
# editar production/terraform.tfvars
```
